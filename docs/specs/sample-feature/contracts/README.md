# Contracts: Sample Feature

This sample feature has no REST, GraphQL, gRPC, event, or database contract.

If a future feature changes an API or event shape, add the relevant contract files here, for example:

- `openapi.yaml`
- `asyncapi.yaml`
- `*.proto`
